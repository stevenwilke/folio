# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# Local Expo state
.expo/

# iOS build artifacts (Xcode-generated)
ios/build/
ios/Pods/
ios/*.xcworkspace/xcuserdata/
ios/*.xcodeproj/xcuserdata/
ios/*.xcodeproj/project.xcworkspace/xcuserdata/

# Android build artifacts (Gradle-generated)
android/build/
android/app/build/
android/.gradle/
android/local.properties
android/.kotlin/

# Firebase config: the embedded API key is restricted in Google Cloud
# (locked to package + SHA-1), so it's safe to commit per Firebase docs.
# Both paths are committed: top-level for app.json's googleServicesFile,
# android/app/ for the Gradle plugin during cloud builds.

# Google OAuth client credentials downloaded from GCP — must not ship in git.
# Web Client Secret lives in Supabase; iOS/Android client IDs are in app.json.
client_secret_*.json
client_*.apps.googleusercontent.com.plist
GoogleService-Info.plist

# Node
node_modules/

# Logs
*.log
